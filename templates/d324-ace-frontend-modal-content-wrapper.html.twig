{#
/**
 * @file
 * Default theme implementation to display a D324 Ace Frontend Modal Content Wrapper.
 *
 * Available variables:
 * - element: The rendered paragraph form/content and all eventual other children.
 *   Use {{ content }} to print them all, or print a subset such as
 *   {{ content.field_example }}. Use {{ content|without('field_example') }}
 *   to temporarily suppress the printing of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - d324-ace-frontend-modal-content-wrapper: The current template type (also known as a "theming hook").
 *
 * @see template_preprocess_d324_ace_frontend_modal_content_wrapper()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes }}>
  {{ content }}
</div>
